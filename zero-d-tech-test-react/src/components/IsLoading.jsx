import React from "react";

const IsLoading = () => {
  //simple message to let the client know the page is still retrieving all the data before it can be rendered
  return <h3> Loading...</h3>;
};

export default IsLoading;
